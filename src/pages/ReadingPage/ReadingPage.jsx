import Container from '../../components/common/Container/Container';

import s from './ReadingPage.module.css';

const ReadingPage = () => {
  return (
    <section className={s.hero}>
      <Container className="reading-page-container"></Container>
    </section>
  );
};

export default ReadingPage;
